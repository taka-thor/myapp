development:
  adapter: async

test:
  adapter: test

production:
  adapter: async   # ← Redis を使わないならこれでOK
  # adapter: redis  # ← Redis を使うならこちらに切り替え
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  # channel_prefix: myapp_production
